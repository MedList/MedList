Created to get poster from tconst(imdb identifiers)

Features:

- Convert **IMDb ID → TMDB ID**
- Retrieve **poster URL** 

Example Usage:

    poster = get_poster_from_imdb("tconst")
    print(poster)

Simply replace the tconst with actual real imdb identifiers